# `logicpearl`

This package name is reserved for the LogicPearl project.

Today, the public LogicPearl surfaces are:
- the Rust CLI in the main repository
- the browser runtime package `@logicpearl/browser` on npm

Future Python-facing support may land here.

For now, this package is intentionally minimal and should not be treated as a full Python SDK.
