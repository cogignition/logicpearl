# WAF Scripts

These scripts build the public WAF benchmark corpus and the small WAF demo slice from staged public datasets.

The scripts expect:

- `CSIC HTTP 2010`
- `OWASP ModSecurity 2025`

under `LOGICPEARL_DATASETS`, using the layout documented in [benchmarks/waf/README.md](../../benchmarks/waf/README.md).
